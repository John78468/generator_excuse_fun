
<div class="w-full max-w-lg mx-auto bg-white p-8 rounded-lg shadow-lg">
  <h2 class="text-2xl font-bold text-blue-700 mb-6 text-center">Ajouter une nouvelle excuse</h2>

  <%= form_with model: @excuse, local: true, class: 'space-y-4' do |form| %>
    <div>
      <%= form.label :part1, "Début de l'excuse", class: 'block text-gray-700' %>
      <%= form.text_field :part1, class: 'w-full px-4 py-2 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500' %>
    </div>

    <div>
      <%= form.label :part2, "Milieu de l'excuse", class: 'block text-gray-700' %>
      <%= form.text_field :part2, class: 'w-full px-4 py-2 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500' %>
    </div>

    <div>
      <%= form.label :part3, "Fin de l'excuse", class: 'block text-gray-700' %>
      <%= form.text_field :part3, class: 'w-full px-4 py-2 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500' %>
    </div>

    <div>
      <%= form.submit "Ajouter l'excuse", class: 'w-full bg-blue-500 text-white px-4 py-2 mt-4 rounded-lg font-semibold hover:bg-blue-600 transition' %>
    </div>
  <% end %>

  <%= link_to 'Retour', random_path, class: 'block text-center mt-4 text-blue-500 hover:underline' %>
</div>
